pipeline {
    agent any

    stages {
        stage('pull') {
            steps {
                     echo '"welcome"'   
                     }
                  }
        stage('build') {
            steps {
                echo 'build-job'
            }
        }
         stage('test') {
            steps {
                echo 'test-job'
            }
        }
        stage('deploy') {
            steps {
                echo 'deploy-job'
            }
        }
    }
}
